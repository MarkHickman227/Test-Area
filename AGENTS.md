# AGENTS.md

## Cursor Cloud specific instructions

This is an empty repository (`Test-Area`) with no application code, dependencies, or services. The only file is `README.md`.

- **No build/lint/test commands** exist yet. Once application code is added, update this section with the relevant commands.
- **No services** need to be started.
- **No environment variables or secrets** are required.
- The update script is a no-op since there are no dependencies to install.

When application code is added to this repository, update:
1. The update script (via `SetupVmEnvironment`) with dependency installation commands.
2. This file with build, lint, test, and run instructions.
